name: Build and Push Docker Image

on:
  push:
    branches:
      - infra-prod # Trigger on pushes to the main branch
  #pull_request:  # Trigger on pull requests

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up Docker
      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: infracdo # ${{ secrets.DOCKER_USERNAME }} # Add DOCKER_USERNAME secret in GitHub
          password: dckr_pat__OLCfPrg82w1BYMpRc0aQbVzV1M # ${{ secrets.DOCKER_PASSWORD }} # Add DOCKER_PASSWORD secret in GitHub

      # Step 3: Build the Docker image
      - name: Build Docker Image
        run: 
          # docker build --target build -t my-image:build .

          docker build -t hiveconnect-frontend:prod-1.1 .

      # Step 4: Push the Docker image
      - name: Push Docker Image
        run: |
          docker tag hiveconnect-frontend:prod-1.1 infracdo/hiveconnect-frontend:prod-1.1
          docker push infracdo/hiveconnect-frontend:prod-1.1
